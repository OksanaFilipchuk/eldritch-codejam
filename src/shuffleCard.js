import ancientsData from './data/ancients';

const ancientActive = document.querySelectorAll(".ancients-img.active");
const shuffleButton = document.querySelector('.shuffle');
const ancientActiveId = ancientActive.getAttribute("id");